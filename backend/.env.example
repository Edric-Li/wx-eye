# WxEye 配置文件示例
# 复制此文件为 .env 并填入你的配置

# ============ Claude AI 配置 ============
# 必填：Anthropic API Key
ANTHROPIC_API_KEY=sk-ant-xxxxx

# 可选：自定义 API 地址（如使用代理或第三方服务）
# ANTHROPIC_BASE_URL=https://api.anthropic.com

# 模型选择: haiku (最快最便宜) / sonnet (平衡) / opus (最强)
CLAUDE_MODEL=sonnet

# ============ OCR 配置 ============
# 支持的语言，逗号分隔。常用: ch_sim(简中), ch_tra(繁中), en(英文)
OCR_LANGUAGES=ch_sim,en

# 是否使用 GPU 加速 OCR (需要 CUDA)
OCR_GPU=false

# ============ AI 处理配置 ============
# 是否启用 AI 分析
ENABLE_AI=true

# 是否启用 OCR 过滤（推荐开启，可节省 90%+ API 成本）
ENABLE_OCR_FILTER=true

# ============ 服务器配置 ============
HOST=0.0.0.0
PORT=8000
DEBUG=false

# ============ 截图配置 ============
SCREENSHOT_DIR=static/screenshots
CAPTURE_INTERVAL=0.1
